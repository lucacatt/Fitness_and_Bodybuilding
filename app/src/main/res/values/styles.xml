<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- ... -->
    </style>
    <style name="MyButton" parent="Widget.MaterialComponents.Button">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:textSize">16sp</item> <!-- Dimensione del testo -->
        <item name="android:textColor">@color/white</item> <!-- Colore del testo -->
        <item name="android:backgroundTint">@color/colorPrimary</item> <!-- Colore di sfondo -->
        <item name="android:padding">16dp</item> <!-- Padding -->
        <item name="cornerRadius">10dp</item> <!-- Angoli arrotondati -->
    </style>
</resources>
